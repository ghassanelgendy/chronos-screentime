<Window x:Class="chronos_screentime.Windows.ThemedMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chronos" Height="200" Width="400"
        Background="#F5F5F5" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock x:Name="TitleText" Text="Message" FontSize="16" FontWeight="Bold" 
                      Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <TextBlock x:Name="IconText" Text="ℹ️" FontSize="24" Margin="0,0,15,0" VerticalAlignment="Top"/>
                <TextBlock x:Name="MessageText" Text="Message content goes here" 
                          FontSize="14" Foreground="#2C3E50" TextWrapping="Wrap" 
                          VerticalAlignment="Center" MaxWidth="300"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Buttons -->
        <Border Grid.Row="2" Background="#ECF0F1" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Buttons will be added dynamically -->
            </StackPanel>
        </Border>
    </Grid>
</Window> 